export default function ChatApp({ chatHistory } : any) {
    return (
      <div>
        {/* {chatHistory.map(message  => (
          <div key={message.id}>
            <p>{message.name}</p>
            <p>{message.body}</p>
            </div>
        ))} */}
      </div>
    );
  }